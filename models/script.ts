import { read, WriteStream } from "fs";

const a = 'arquivo.dat';


interface Archive {

Product : String,
User : String


}

async function readFileDat( arquive : Archive){
     
   const new_archive =  ReadableStream.archive(a){
        const write_a = WriteStream(a);
        const read_a = ReadableStream(a);


        for (let i = 0; i < new_archive.length ; i++){
           

           read_a(a);

           if (a[i] == ""){

           }
        }
    }
    return new_arquive;
 
}

async function OutPutFile(arquive_descompacted){
    try {
        


    }catch(err){
        console.log("cannot export the new archive" + err);
    }
}